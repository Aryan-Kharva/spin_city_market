<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Spin City Market - Edit Form</title>

    <link rel="stylesheet" href="/assets/css/new.css" />
</head>
<body id="body-items">
    <!-- Header -->
    <%- include('partials/header') %>

    <main>
        <div class="form-container">
            <img class="center" src="/assets/images/header3.png" alt="Edit Vinyl Record" />
            <form action="/items/<%= item.id %>" method="POST" enctype="multipart/form-data" class="edit-item-form">
                <div>
                    <label for="condition">Condition</label>
                    <select id="condition" name="condition" required>
                        <option value="">-- Select Condition --</option>
                        <option value="Mint" <%= item.condition === "Mint" ? "selected" : "" %>>Mint</option>
                        <option value="Near Mint" <%= item.condition === "Near Mint" ? "selected" : "" %>>Near Mint</option>
                        <option value="Very Good" <%= item.condition === "Very Good" ? "selected" : "" %>>Very Good</option>
                        <option value="Good" <%= item.condition === "Good" ? "selected" : "" %>>Good</option>
                        <option value="Poor" <%= item.condition === "Poor" ? "selected" : "" %>>Poor</option>
                    </select>
                </div>

                <div>
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" placeholder="Enter Title" value="<%= item.title %>" required>
                </div>

                <div>
                    <label for="artist">Artist</label>
                    <input type="text" id="artist" name="artist" placeholder="Enter Artist Name" value="<%= item.artist %>" required>
                </div>

                <!-- <div>
                    <label for="seller">Seller</label>
                    <input type="text" id="seller" name="seller" placeholder="Enter Seller Name" value="<%= item.seller %>" required>
                </div> -->

                <div>
                    <label for="price">Price</label>
                    <input type="number" id="price" name="price" min="0" step="0.01" placeholder="0.00" value="<%= item.price %>" required>
                </div>

                <div>
                    <label for="details">Details</label>
                    <textarea id="details" name="details" rows="5" placeholder="Enter Details" required><%= item.details %></textarea>
                </div>

                <div>
                    <label for="image">Image</label>
                    <input type="file" id="image" name="image" accept="image/*">
                    <p>Current Image:</p>
                    <img src="/<%= item.image %>" alt="<%= item.title %>" style="width: 150px;">
                </div>

                <button type="submit">Save Changes</button>
            </form>
        </div>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>
</body>
</html>
